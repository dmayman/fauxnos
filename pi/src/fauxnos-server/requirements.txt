# Fauxnos Server Dependencies
# ============================
# Install with: pip3 install -r requirements.txt

# Core web framework for API server
flask>=2.0.0

# YAML configuration template support
pyyaml>=6.0

# WebSocket client for go-librespot event streaming
websockets>=10.0

# HTTP requests for API calls
requests>=2.28.0

# SSH client for server-driven client install (Add Device wizard)
paramiko>=3.4

# Optional: Enhanced JSON support (for future use)
# jsonschema>=4.0.0

# Development dependencies (install separately if needed)
# pytest>=7.0.0
# pytest-cov>=4.0.0